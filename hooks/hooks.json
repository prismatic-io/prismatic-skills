{
  "description": "Enforce tool usage policies for Prismatic integration building",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__prism__prism_components_list",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"permissionDecision\":\"deny\"},\"systemMessage\":\"BLOCKED: Use the search script instead: npx tsx ${CLAUDE_PLUGIN_ROOT}/scripts/integrations/find-components.ts <keyword>\"}' >&2 && exit 2",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
